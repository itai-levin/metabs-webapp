version: "3.8"

services:
  webapp:
    build:
      context: .
    # publish ONLY to localhost so host nginx can reverse proxy to it
    ports:
      - "127.0.0.1:8501:8501"
    restart: unless-stopped
